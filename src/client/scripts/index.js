import React from 'react';
import ReactDOM from 'react-dom';
import AppRoot from '../../app/core/AppRoot.jsx';

ReactDOM.render(
    React.createElement(AppRoot),
    document.getElementById('root')
);
